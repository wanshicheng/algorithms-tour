package com.skillip.algorithms.merge;

public class InPlaceMerge {
    public static void merge(Comparable[] a, int lo, int mid, int hi) {
        int i = lo;
        int j = mid + 1;

        for (int k = lo; k <= hi; k++) {
        }
    }
}
